const ToolStorage = require('../shared/tool-storage');

module.exports = async function(toolInput) {
  const { title, date } = toolInput;
  // TO DO: implement the actual logic for planning grid with export here.
};