const tools = [
  // other tools
  {
    id: 'balance-dashboard',
    name: 'Balance Dashboard',
    description: 'Analyze data and render verdicts, issues, and charts.',
    category: 'dashboard',
    audience: 'all',
    inputs: [],
    outputs: [],
    tags: ['data', 'analysis'],
    run: () => {
      try {
        // existing code
      } catch (error) {
        console.error('Error in balance-dashboard:', error);
      }
    }
  },
  {
    id: 'meta-health-dashboard',
    name: 'Meta Health Dashboard',
    description: 'Run the engine and perform specific tasks related to meta health dashboard functionality.',
    category: 'dashboard',
    audience: 'all',
    inputs: [],
    outputs: [],
    tags: ['engine', 'health'],
    run: () => {
      try {
        // existing code
      } catch (error) {
        console.error('Error in meta-health-dashboard:', error);
      }
    }
  },
  {
    id: 'mission-section',
    name: 'Mission Section',
    description: 'Render a section of the user interface for mission-related content.',
    category: 'dashboard',
    audience: 'all',
    inputs: [],
    outputs: [],
    tags: ['ui', 'mission'],
    run: () => {
      try {
        // existing code
      } catch (error) {
        console.error('Error in mission-section:', error);
      }
    }
  }
];